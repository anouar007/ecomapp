1 : git clone https://github.com/anouar007/ecomapp.git .
2 : git checkout jawhara
3 : composer install --no-dev --optimize-autoloader
4 : php artisan key:generate
5 : php artisan migrate --force
6 : php artisan db:seed --class=RolePermissionSeeder
7 : php artisan db:seed --class=SettingsSeeder
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
export NVM_DIR="$HOME/.nvm" 
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm install --lts
npm install
npm run build
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
ln -s ../storage/app/public public/storage
php artisan make:admin your-email@domain.com YourPassword123 --name="Admin"
